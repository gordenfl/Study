#include <iostream>
using std::cout;
using std::endl;;
int main() {
    int port = 12345;  // 服务器监听的端口

    // 创建服务器对象，传入监听的端口号
    //ChatServer server(port);

    // 启动服务器
    cout<< "Hello World" << endl;

    return 0;
}